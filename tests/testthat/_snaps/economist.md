# theme_economist_white is deprecated

    Code
      x <- theme_economist_white()
    Condition
      Warning:
      `theme_economist_white()` was deprecated in ggthemes 7.0.0.
      i Please use `theme_economist()` instead.
      i The current design already draws a white panel, so the white variant no longer differs from `theme_economist()`.

# theme_economist(dkpanel=) is deprecated

    Code
      x <- theme_economist(dkpanel = TRUE)
    Condition
      Warning:
      The `dkpanel` argument of `theme_economist()` is deprecated as of ggthemes 7.0.0.
      i The darker panel belonged to the pre-2017 design, which this theme no longer implements.

# economist_pal(fill=) is deprecated

    Code
      x <- economist_pal(fill = TRUE)
    Condition
      Warning:
      The `fill` argument of `economist_pal()` is deprecated as of ggthemes 7.0.0.
      i The Economist's current chart palette draws no distinction between fill and colour, so `fill` no longer has any effect.

