# theme_pander warns about ff argument

    Code
      x <- theme_pander(ff = "")
    Condition
      Warning:
      `ff` is deprecated. Use `base_family` instead.

# theme_pander warns about fs argument

    Code
      x <- theme_pander(fs = 1)
    Condition
      Warning:
      `fs` is deprecated. Use `base_size` instead.

