# ptol_pal is deprecated in favour of khroma

    Code
      p <- ptol_pal()
    Condition
      Warning:
      `ptol_pal()` was deprecated in ggthemes 7.0.0.
      i This palette is the 12-colour qualitative scheme from Paul Tol's 2012 technical note. He has revised his schemes since; the current ones are at <https://sronpersonalpages.nl/~pault/>.
      i The khroma package tracks those revisions. The closest successor to this palette is `khroma::colour("muted")`, or `khroma::scale_colour_muted()` for a ggplot2 scale.

# scale_colour_ptol is deprecated in favour of khroma

    Code
      s <- scale_colour_ptol()
    Condition
      Warning:
      `scale_colour_ptol()` was deprecated in ggthemes 7.0.0.
      i This palette is the 12-colour qualitative scheme from Paul Tol's 2012 technical note. He has revised his schemes since; the current ones are at <https://sronpersonalpages.nl/~pault/>.
      i The khroma package tracks those revisions. The closest successor to this palette is `khroma::colour("muted")`, or `khroma::scale_colour_muted()` for a ggplot2 scale.

# scale_color_ptol is deprecated in favour of khroma

    Code
      s <- scale_color_ptol()
    Condition
      Warning:
      `scale_colour_ptol()` was deprecated in ggthemes 7.0.0.
      i This palette is the 12-colour qualitative scheme from Paul Tol's 2012 technical note. He has revised his schemes since; the current ones are at <https://sronpersonalpages.nl/~pault/>.
      i The khroma package tracks those revisions. The closest successor to this palette is `khroma::colour("muted")`, or `khroma::scale_colour_muted()` for a ggplot2 scale.

# scale_fill_ptol is deprecated in favour of khroma

    Code
      s <- scale_fill_ptol()
    Condition
      Warning:
      `scale_fill_ptol()` was deprecated in ggthemes 7.0.0.
      i This palette is the 12-colour qualitative scheme from Paul Tol's 2012 technical note. He has revised his schemes since; the current ones are at <https://sronpersonalpages.nl/~pault/>.
      i The khroma package tracks those revisions. The closest successor to this palette is `khroma::colour("muted")`, or `khroma::scale_colour_muted()` for a ggplot2 scale.

